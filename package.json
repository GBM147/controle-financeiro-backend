{
  "name": "controle-gastos-backend",
  "version": "1.0.0",
  "description": "API e site do GBM Finance para organização financeira pessoal.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check pdf-extrato-parser.js && node --check pdf-render.js && node --check ofx-parser.js && node --check file-signatures.js && node --check session-policy.js && node --check public/auth.js && node --check public/gbm-pages.js && node --check public/gbm-tutorial.js && node --check public/sw.js",
    "lint": "npm run check",
    "test": "node --test",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "type": "commonjs",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-mysql-session": "^3.0.3",
    "express-rate-limit": "^8.6.2",
    "express-session": "^1.18.1",
    "helmet": "^8.3.0",
    "mercadopago": "^3.0.0",
    "multer": "^2.2.0",
    "mysql2": "^3.23.3",
    "node-cron": "^4.6.0",
    "pdf-parse": "^1.1.4",
    "resend": "^6.19.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
