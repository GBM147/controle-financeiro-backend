/* Compatibilidade legada.
   O padrão visual único é carregado por gbm-pages.css + gbm-pages.js.
   Este arquivo permanece sem estilos para evitar regressões quando um cache antigo
   ou service worker ainda o injetar no Dashboard. */
(function () {
    'use strict';
})();
